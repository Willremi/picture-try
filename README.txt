# picture-try

Test de création de branche sur github
git checkout master
git pull
git checkout -b test-hello
git push --set-upstream origin test-hello (pusher la première fois)
git push (les fois d'après)

Supprimer la branche
git branch -d test-hello
